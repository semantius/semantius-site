---
import Button from '~/components/ui/Button.astro';
import Badge from '~/components/ui/Badge.astro';

interface Props {
  badge?: string;
  title?: string;
  highlight?: string;
  description?: string;
  primaryCta?: { text: string; href: string };
  secondaryCta?: { text: string; href: string };
}

const {
  badge = 'The Open Source Agentic Data Platform',
  title = 'Model your domain,',
  highlight = 'deploy the platform.',
  description = 'Define your domain model and deploy a governed PostgreSQL backend: REST APIs, an admin UI for your team, and guardrails for your agents.',
  primaryCta = { text: 'Get Started', href: "/docs" },
  secondaryCta = { text: 'Check our features', href: "/docs/reference/overview" }
} = Astro.props;
---

<section class="relative overflow-hidden pt-32 pb-16 md:pt-40 md:pb-20 bg-background">
  <div class="absolute inset-0 z-0 opacity-40">
     <div class="absolute top-[20%] left-[20%] w-[500px] h-[500px] bg-blue-900/40 rounded-full blur-[128px] animate-pulse"></div>
     <div class="absolute bottom-[20%] right-[20%] w-[500px] h-[500px] bg-indigo-900/40 rounded-full blur-[128px] animate-pulse" style="animation-delay: 2s;"></div>
  </div>

  <div class="container mx-auto px-6 relative z-10 text-center">
    <div class="hero-content opacity-0 translate-y-5">
      <Badge class="mb-6 border-foreground/10">
        {badge}
      </Badge>
      <h1 class="text-5xl md:text-7xl font-bold tracking-tight mb-8 text-foreground">
        {title} <br />
        <span class="text-primary">
           {highlight}
        </span>
      </h1>
      <p class="text-xl text-foreground/70 max-w-2xl mx-auto mb-10">
        {description}
      </p>
      
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <Button
          href={primaryCta.href}
          class="px-8 py-4 rounded-full text-base"
          aria-label={`${primaryCta.text} - ${title} ${highlight}`}
          data-ph-event="hero_cta_clicked"
          data-ph-cta={primaryCta.text}
          data-ph-variant="primary"
        >
          {primaryCta.text}
        </Button>
        <Button
          href={secondaryCta.href}
          variant="secondary"
          class="px-8 py-4 rounded-full text-base backdrop-blur-sm"
          aria-label={`${secondaryCta.text} about our features`}
          data-ph-event="hero_cta_clicked"
          data-ph-cta={secondaryCta.text}
          data-ph-variant="secondary"
        >
          {secondaryCta.text}
        </Button>
      </div>
    </div>
  </div>
</section>

<script is:inline>
  document.querySelectorAll('[data-ph-event="hero_cta_clicked"]').forEach(function(el) {
    el.addEventListener('click', function() {
      window.posthog?.capture('hero_cta_clicked', {
        cta_label: el.getAttribute('data-ph-cta'),
        variant: el.getAttribute('data-ph-variant')
      });
    });
  });
</script>

<style>
  .hero-content {
    animation: fadeUp 0.8s ease-out forwards;
  }

  @keyframes fadeUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
